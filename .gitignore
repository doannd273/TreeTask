# IntelliJ / Android Studio
*.iml
.gradle/
.idea/
.vscode/
.kotlin/

# Local Configuration (KHÔNG BAO GIỜ PUSH)
local.properties

# Build Artifacts (KHÔNG BAO GIỜ PUSH - Sẽ làm Git phình to hàng chục GB)
build/
**/build/
/build

# Signing (KHÔNG BAO GIỜ PUSH - Mất chìa khoá là mất luôn App)
*.jks
*.keystore
*.p8
*.p12
key.properties

# Firebase / Secret files (Rất hay bị lộ nếu Repo là Public)
firebase_app_id_file.json
GoogleService-Info.plist

# Mac OS & OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Native Android files
.externalNativeBuild
.cxx/
/captures

# Room DB Schema (NÊN COMMIT để track lịch sử migration)
# Nếu bạn muốn ignore thì bỏ dấu # ở dòng dưới
# **/schemas/

# Google Services (CI sẽ inject từ GitHub Secrets khi build)
google-services.json
**/google-services.json
